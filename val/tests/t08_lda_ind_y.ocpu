    ; create pointer at zp $20/$21 -> points to $0030 (avoids code overlap)
    lda #$2b
    sta $0020
    lda #$00
    sta $0021
    ldy #$05
    lda ($20),y
    jmp done

    .org $0030
    .byte $55

    .org $0100
done:
    jmp done
