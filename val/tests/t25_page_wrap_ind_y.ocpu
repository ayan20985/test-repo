    ; ind,y: zero-page pointer at $30/$31 points to $0200, Y=0, read stored value
    lda #$20
    sta $0200
    lda #$00
    sta $0030
    lda #$08
    sta $0031
    ldy #$00
    lda ($30),y
    jmp done

    .org $0100
done:
    jmp done
